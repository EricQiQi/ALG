# 👑 滑动窗口最大值：单调队列与分块 DP 双战神版指南

力扣 **239. 滑动窗口最大值（Hard）** 是一道著名的“披着羊皮的狼”。如果使用常规的滑动窗口在窗口内“翻箱倒柜”找最大值，时间复杂度会飙升至 $O(N \times K)$，在面对 $N=100,000$ 的大数据集时会**毫无悬念地超时（TLE）**。

要实现真正的 **$O(N)$ 硬件级速度**，行业内有两种统治级的写法语路：一种是利用**单调队列**进行无情内卷，另一种是利用**动态规划（DP）**进行分块查表。本篇文档为你将这两种战神级 Java 解法打包收录。

---

## 🏃 战神解法一：单调队列法（最强工具人：时间 $O(N)$，空间 $O(K)$）

这个解法通过引入一个双端队列（`Deque`），把窗口区域变成了一个**“职场内卷/长江后浪推前浪”**的游戏。

### 🎮 大白话核心机制
1. **帮规：严格单调递减**
   队列里存的是柱子的**下标（索引）**。从队头到队尾，对应的柱子高度必须是严格从大到小死死卡住的。队头永远坐着当前窗口的“带头大哥”（最大值）。
2. **新人入队（无情内卷）**
   新柱子（新人）进窗时，从队尾看过去。如果队尾的人**比新人都矮**，说明只要有新人在，那些又老又矮的人就永远不可能成为窗口最大值了。新人会无情地把队尾比他矮的人通通踢走（`pollLast()`），直到碰壁或队列空了才坐下。
   👉 *大白话：只要你比我新，还比我强，那你就把我内卷了，我可以当场退休了。*
3. **老大退役（年龄到期）**
   窗口右移，老大哥虽然很强，但他的物理位置可能已经滑出窗口了。检查队头大哥的下标，若发现落后于 `right - k + 1`，强制赶走退役（`pollFirst()`）。

### 💻 Java 代码实现
```java
import java.util.ArrayDeque;
import java.util.Deque;

public class SolutionQueue {
    public int[] maxSlidingWindow(int[] nums, int k) {
        if (nums == null || nums.length == 0 || k <= 0) return new int[0];
        
        int n = nums.length;
        int[] ans = new int[n - k + 1]; // 结果数组
        int r = 0; // 结果数组指针
        
        // 核心武器：双端队列，用来维护“单调递减”的下标
        Deque<Integer> deque = new ArrayDeque<>();
        
        for (int right = 0; right < n; right++) {
            // 1. 【新人入队】：只要队尾的人比当前新人矮，通通踢走，无情内卷！
            while (!deque.isEmpty() && nums[deque.peekLast()] <= nums[right]) {
                deque.pollLast();
            }
            
            // 2. 新人安全入队尾
            deque.addLast(right);
            
            // 3. 【老大退役】：如果队头大哥的下标已经过期（滑出窗口左边界），强制退役！
            if (deque.peekFirst() < right - k + 1) {
                deque.pollFirst();
            }
            
            // 4. 窗口正式成型（走了至少 k-1 步），队头就是无敌最大值
            if (right >= k - 1) {
                ans[r++] = nums[deque.peekFirst()];
            }
        }
        return ans;
    }
}
```

---

## 🧱 战神解法二：分块双向 DP 法（极致数学美：时间 $O(N)$，空间 $O(N)$）

如果你觉得单调队列的内卷逻辑、双端队列指针容易写出 Bug，那么这套基于**动态规划（DP）**思想的“分班双向扫描法”则是工程稳定性的神作。

### 🎮 大白话核心机制
1. **化整为零：划分小班级**
   既然几万人在大队伍里找老大很慢，我们把 10 万人按照每 $K$ 个人一组，分成一个个“小班级（块）”。
2. **两座单调大坝（状态转移方程）**
    * `left_max[i]`：记录从自己**班级开头**走到位置 `i` 的最强者。
      👉 `left_max[i] = (i % k == 0) ? nums[i] : Math.max(left_max[i - 1], nums[i])`
    * `right_max[i]`：从右往左倒着扫，记录从当前位置到自己**班级结尾**的最强者。
3. **降维打击：一秒查表**
   当一个滑动窗口跨越两个班级时（前半截在 A 班，后半截在 B 班）：
    * 跨在 A 班的这一截，最大值必定是 `right_max[left]`（从左端点到 A 班结尾的最大值）。
    * 跨在 B 班的这一截，最大值固定是 `left_max[right]`（从 B 班开头到右端点的最大值）。
    * 整个窗口的老大，直接就是两者的较大值：`Math.max(right_max[left], left_max[right])`！

### 💻 Java 代码实现
```java
public class SolutionDP {
    public int[] maxSlidingWindow(int[] nums, int k) {
        if (nums == null || nums.length == 0 || k <= 0) return new int[0];
        int n = nums.length;
        int[] res = new int[n - k + 1];

        // 记账本：盖两座每 K 个元素自动断开重新筑基的分段大坝
        int[] left_max = new int[n];
        int[] right_max = new int[n];

        // 1. 正向状态转移：算出每个位置在自己班级里，从左到当前的最高分
        for (int i = 0; i < n; i++) {
            if (i % k == 0) {
                left_max[i] = nums[i]; // 班级第一个人
            } else {
                left_max[i] = Math.max(left_max[i - 1], nums[i]);
            }
        }

        // 2. 逆向状态转移：算出每个位置在自己班级里，从当前到结尾的最高分
        for (int i = n - 1; i >= 0; i--) {
            if (i == n - 1 || (i + 1) % k == 0) {
                right_max[i] = nums[i]; // 班级最后一个人
            } else {
                right_max[i] = Math.max(right_max[i + 1], nums[i]);
            }
        }

        // 3. 一键出答案：完全消灭嵌套循环，利用 DP 状态进行 O(1) 合并
        for (int i = 0; i < res.length; i++) {
            int left = i;
            int right = i + k - 1;
            res[i] = Math.max(right_max[left], left_max[right]);
        }

        return res;
    }
}
```
# 深度图解：为什么双向分块拼接正好是窗口最大值？

为了彻底理解 `res[i] = Math.max(right_max[left], left_max[right])` 这行代码，我们需要从**几何空间拼接**和**具体数字演练**两个维度进行剖析。

---

## 一、 空间拼接原理解析

### 1. 核心现象：窗口必然跨块
在原数组被切分成长度为 \(k\) 的固定“块（Blocks）”后，除了窗口刚好与块的边界完全重合的特殊情况外，**任意一个长度为 \(k\) 的滑动窗口，必然会同时横跨两个相邻的块**。

我们将前一个块称为 **块 A**，后一个块称为 **块 B**。

### 2. 完美的空间重合度
假设当前窗口的左边界在 `left`，右边界在 `right`：

```text
               块 A (前一个块)             │             块 B (后一个块)
       ┌─────────────────────────────────┐ │ ┌─────────────────────────────────┐
块索引: │ 块开头 ...  [left   ...   块末尾] │ │ │ [块开头   ...   right] ... 块末尾 │
       └───────────────┬─────────────────┘ │ └─────────────┬───────────────────┘
                       │                   │               │
                       └─────────── 整个滑动窗口 (长度为 k) ────────────┘
```

根据预处理数组的定义：
*   **`right_max[left]`**：求的是从 `left` 到 **块 A 末尾** 这一段区间的最大值。
*   **`left_max[right]`**：求的是从 **块 B 开头** 到 `right` 这一段区间的最大值。

**巧妙之处就在这里：**
因为整个窗口的长度是固定的 $k$，当窗口从块 A 漏出多少（即进入块 B 多少），块 A 内部就会剩下多少。
*   `[left ... 块 A 末尾]` 构成了窗口的**左半部分**。
*   `[块 B 开头 ... right]` 构成了窗口的**右半部分**。

这两部分在物理位置上**无缝拼接**，合起来的空间正好**不多不少，完美等于整个滑动窗口 `[left, right]`**。因此，两者的最大值进行比较，结果必然是整个窗口的最大值。

---

## 二、 具体数字全流程推导

我们用一个具体实例来模拟计算机的运行过程：
*   原数组 `nums = [1, 3, -1, -3, 5, 3]`
*   窗口大小 `k = 3`

### 1. 数组分块
按 $k = 3$ 划分，原数组被分成 2 个块：
*   **块 1**：`[1, 3, -1]` （对应索引 0, 1, 2）
*   **块 2**：`[-3, 5, 3]` （对应索引 3, 4, 5）

### 2. 预处理出的双向最值数组
根据分块规则，分别从左向右（前缀）、从右向左（后缀）计算最值：

*   **`left_max`（每到块开头重置）**：
*   块 1：`[1, 3, 3]`  *(3是1和3的最值，最后一个3是1,3,-1的最值)*
*   块 2：`[-3, 5, 5]` *(5是-3和5的最值，最后一个5是-3,5,3的最值)*
*   结果：`left_max = [1, 3, 3, -3, 5, 5]`

*   **`right_max`（每到块末尾重置）**：
*   块 1：`[3, 3, -1]` *(从后往前：-1，然后3和-1比得3，然后1和3比得3)*
*   块 2：`[5, 5, 3]`  *(从后往前：3，然后5和3比得5，然后-3和5比得5)*
*   结果：`right_max = [3, 3, -1, 5, 5, 3]`

### 3. 实战模拟查表：以第 3 个窗口为例
当窗口滑动到第 3 个位置时，索引信息如下：
*   `i = 2`
*   `left = i = 2`（对应元素 `-1`，位于**块 1 末尾**）
*   `right = i + k - 1 = 4`（对应元素 `5`，位于**块 2 中间**）

此时的真实窗口区间是 `nums[2 ~ 4]`，即 **`[-1, -3, 5]`**。

#### 一键查表计算：
$$\begin{aligned}
res[2] &= Math.max(right\_max[left], left\_max[right]) \\
&= Math.max(right\_max[2], left\_max[4])
\end{aligned}$$

1.  **查表 `right_max[2]`**：
*   含义：块 1 中从索引 2 到块末尾的最值。
*   实际值：`right_max[2] = -1`（即窗口左半部分的真实最值）。
2.  **查表 `left_max[4]`**：
*   含义：块 2 中从块开头到索引 4 的最值。
*   实际值：`left_max[4] = 5`（即窗口右半部分 `[-3, 5]` 的真实最值）。

#### 最终合并：
$$res[2] = Math.max(-1, 5) = 5$$

**结论：** 真实窗口 `[-1, -3, 5]` 的最大值确实是 `5`。算法不需要任何循环去遍历窗口，仅靠两次查表和一次比大小，在 $O(1)$ 时间内精准锁定了答案！

---

## 📊 双战神解法终极横向对比

| 维度 | 🏃 战神解法一（单调队列法） | 🧱 战神解法二（分块双向 DP 法） |
| :--- | :--- | :--- |
| **底层核心思想** | 贪心思想、实时维护单调递减队列 | **动态规划（DP）**、子问题预处理与状态合并 |
| **时间复杂度** | **$O(N)$**（每个下标最多进出队列各 1 次） | **$O(N)$**（三个独立的单层 `for` 循环） |
| **空间复杂度** | **$O(K)$**（队列最多同时存 $K$ 个元素） | $O(N)$（需要开辟两个长度为 $N$ 的大坝数组） |
| **编码复杂度** | 较高（需熟练操作双端队列头尾指针） | 极低（三个极其机械、工整的单层循环） |
| **边界条件坑** | 容易搞错滑出窗口的下标过期逻辑 | 几乎无坑，只需注意 `% k == 0` 的边界筑基 |
| **工程实用度** | 内存开销小，适合运行内存极度受限场景 | 稳定性强，代码无嵌套分支，对 CPU 分支预测极度友好 |

---

## 🗣️ 大厂面试官前的“高级感”结题陈词

当你完成解答后，可以用以下复盘说辞对两种解法进行降维定调：

> “这道大 Hard 题本质上是在解决‘如何在动态区间内消除多余子问题计算’的挑战。
>
> 在工程实践中，我准备了两种 $O(N)$ 复杂度的方案：
> 第一种是**单调队列的贪心内卷方案**。通过维护一个严格单调递减的双端队列，让新元素从队尾对不合规的过时元素进行淘汰，从而将空间复杂度死死卡在 $O(K)$。
>
> 第二种则是更加具备数学美感和工程稳定性的**动态规划分块双向预处理方案**。它在内核上和‘接雨水’的 DP 左右大坝解法异曲同工。通过将全场数据按照窗口 $K$ 划分小块子问题，利用前后向两组状态转移方程，把‘区间检索最大值’这一沉重的负担提前压缩成了 $O(1)$ 的查表操作。虽然它使用了 $O(N)$ 的空间，但它**彻底消灭了代码中的内层嵌套与复杂的队列条件分支**。在底层硬件执行时，能够完美契合 CPU 的指令分支预测，在工程吞吐量表现上极具优势。”

---